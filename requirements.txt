# Core dependencies for RETS Hero
# PySimpleGUI is now the sole GUI backend (fallback to FreeSimpleGUI removed in Oct 2025 refactor).
rets
FreeSimpleGUI

# Optional / environment-specific extras (uncomment if needed):
# IPython   # For richer notebook detection/logging; not required for normal GUI use
